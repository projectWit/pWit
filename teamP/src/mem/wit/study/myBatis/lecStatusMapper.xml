<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mem.wit.study.myBatis.LecStatusMapper">
	<resultMap id="LecStatusMap" type="mem.wit.study.codes.LecStatus">
	</resultMap>
  
  <select id="select" resultType="mem.wit.study.codes.LecStatus">
    SELECT * FROM cdLecStatus
		WHERE lsCode = #{lsCode}
  </select>
  <select id="selectAll" resultMap="mem.wit.study.myBatis.LecStatusMapper.LecStatusMap">
    SELECT * FROM cdLecStatus
  </select>
</mapper>